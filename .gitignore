__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
*.egg-info/
.DS_Store

# config.yaml is meant to be committed — keep real secrets out of it and provide
# them via the environment / Actions secrets instead (see config.yaml). A local
# .env (gitignored above) is a handy place to keep them off the committed file.
