__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
*.egg-info/
.DS_Store

# config.yml may hold secrets (an optional `secrets:` block) — never commit it by
# accident. The committed template is config.example.yml. If you run on GitHub
# Actions, keep secrets OUT of the file, set them as Actions secrets, then add the
# keyless config explicitly with:  git add -f config.yml
config.yml
