# briarPipe runtime dependencies
feedparser>=6.0        # RSS/Atom parsing
trafilatura>=1.8       # readable article-text extraction
httpx>=0.27            # HTTP fetching + the OpenAI-compatible provider client
PyYAML>=6.0            # config + source-base serialization

# Dev / test only
pytest>=8.0
